-- These changes were already applied via Supabase migrations
-- popularity_score column already exists
-- idx_tracks_popularity_score already exists
-- idx_artists_name_lower already exists
-- players already has correct unique constraint on (session_id, name)

-- Empty migration (changes already applied via Supabase)